    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    {{#if csrf_header}}
    <meta name="csrf-token" content="{{csrf_header}}">
    {{/if}}
